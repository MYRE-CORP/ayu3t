import { IsCardButtonDisablePipe } from './is-card-button-disable.pipe';

describe('IsCardButtonDisablePipe', () => {
  it('create an instance', () => {
    const pipe = new IsCardButtonDisablePipe();
    expect(pipe).toBeTruthy();
  });
});
